[pytest]
testpaths = tests
python_files = test_*.py
# These two need network/Alpaca-API access and pilot data path that's different
# in this package layout. Skip in default run.
addopts = --ignore=tests/test_replay_regression.py --ignore=tests/test_pilot_baseline.py
